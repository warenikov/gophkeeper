// Команда client — CLI-приложение GophKeeper.
//
// Оно позволяет пользователю зарегистрироваться/аутентифицироваться на
// удалённом сервере GophKeeper и управлять своими приватными данными (пары
// логин/пароль и произвольный текст в Спринте 1) локально и в синхронизации с
// сервером. Все чувствительные данные шифруются на клиенте, прежде чем покинуть
// машину (модель zero-knowledge).
package main

import "os"

func main() {
	if err := Execute(); err != nil {
		os.Exit(1)
	}
}
